# Secrets / local config
github.properties
local.properties
keystore.properties
.env
.env.*
*.keystore
*.jks
# Shared debug signing key (debug-only, standard android/androiddebugkey
# credentials, no security value) — committed so CI and local debug builds
# share a signature and update each other in place.
!app/debug.keystore

# Build output
.gradle/
.kotlin/
build/
node_modules/
bridge/data/

# IDE
*.iml
.idea/

# OS
.DS_Store

# Decompiled reference APKs (never commit)
references/
