<?xml version="1.0" encoding="utf-8"?>
<dev.oneuiproject.oneui.widget.RoundedLinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/schedule_trash_bar"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="bottom"
    android:layout_marginStart="16dp"
    android:layout_marginEnd="16dp"
    android:layout_marginBottom="20dp"
    android:background="@color/oneui_card_bg"
    android:elevation="6dp"
    android:orientation="horizontal"
    android:paddingStart="8dp"
    android:paddingTop="10dp"
    android:paddingEnd="8dp"
    android:paddingBottom="10dp"
    android:visibility="gone"
    app:roundedCorners="all">

    <LinearLayout
        android:id="@+id/schedule_trash_restore"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:gravity="center"
        android:minHeight="56dp"
        android:orientation="vertical"
        android:paddingTop="4dp"
        android:paddingBottom="4dp">

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/schedule_trash_restore_icon"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:contentDescription="@string/schedule_restore"
            app:srcCompat="@drawable/ic_oui_arrow_to_left"
            app:tint="@color/oneui_text_primary" />

        <TextView
            android:id="@+id/schedule_trash_restore_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="@string/schedule_restore"
            android:textColor="@color/oneui_text_primary"
            android:textSize="12sp" />
    </LinearLayout>

    <LinearLayout
        android:id="@+id/schedule_trash_delete"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:gravity="center"
        android:minHeight="56dp"
        android:orientation="vertical"
        android:paddingTop="4dp"
        android:paddingBottom="4dp">

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/schedule_trash_delete_icon"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:contentDescription="@string/delete"
            app:srcCompat="@drawable/ic_oui_delete_outline"
            app:tint="@color/metric_red" />

        <TextView
            android:id="@+id/schedule_trash_delete_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="@string/delete"
            android:textColor="@color/metric_red"
            android:textSize="12sp" />
    </LinearLayout>
</dev.oneuiproject.oneui.widget.RoundedLinearLayout>
